<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0">
		<channel>
			<title>{{ meta.title }}</title>
				<description>{{ meta.description }}</description>
				<link>{{ BASE_URL }}</link>
					{% for post in posts %}
						<item>
								<title>{{ post.title }}</title>
								<description>{{ post.html_body }}</description>
								<link>{{ BASE_URL }}{{ post.url }}</link>
						</item>
					{% endfor %}
		</channel>
</rss>
